{
  "name": "valea-tocului",
  "version": "0.1.0",
  "main": "server.js",
  "scripts": {
    "install-dep": "npm install && cd client && npm install && cd ../",
    "client": "cd client && npm run start",
    "start": "cd client && npm run build && cd ../ && nodemon server.js"
  },
  "author": "Brad Traversy",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^1.14.6"
  },
  "dependencies": {
    "concurrently": "^3.5.1",
    "cors": "^2.8.5",
    "express": "^4.16.2"
  }
}
